 # Copyright (c) 2022, salesforce.com, inc.
 # All rights reserved.
 # SPDX-License-Identifier: BSD-3-Clause
 # For full license text, see the LICENSE file in the repo root or https://opensource.org/licenses/BSD-3-Clause

model:
  arch: trio_t5

  model_type: flant5xl
  load_finetuned: False
  vit_model: "clip_b"
  random_init_qformer: True
  num_frames: 1

datasets:
  minecraft: # name of the dataset builder
    data_type: videos
    total_num_frames: 1
    num_skip_frames: 0
    vis_processor:
        train:
          name: "trio_video_train"
        eval:
          name: "trio_video_eval"
    text_processor:
        train:
          name: "blip_caption"
        eval:
          name: "blip_caption"

run:
  task: video_captioning # TODO: Add associated metrics
  # optimizer
  lr_sched: "linear_warmup_cosine_lr"
  init_lr: 3.0e-6
  min_lr: 0
  weight_decay: 0.05
  max_epoch: 800
  batch_size_train: 1
  batch_size_eval: 1
  num_workers: 1

  # Currently unused args, used for evaluation
  max_len: 20
  min_len: 5
  num_beams: 3

  seed: 42
  output_dir: "output/Trio/Minecraft_Eval"

  # logging

  amp: False
  resume_ckpt_path: "/home/nikepupu/Desktop/LAVIS/lavis/output/Trio/Minecraft/20231101185/checkpoint_40.pth"

  evaluate: False 
  train_splits: ["train"]
  valid_splits: ["train"]
  test_splits: ["train"]

  device: "cuda"
  world_size: 1
  dist_url: "env://"
  distributed: True
